suite: add custom labels and verify that resources get them
templates:
  - deployment.yaml
  - service.yaml
tests:
  - it: should work
    set:
      labels:
        foo: bar
    asserts:
      - equal:
          path: metadata.labels.foo
          value: bar

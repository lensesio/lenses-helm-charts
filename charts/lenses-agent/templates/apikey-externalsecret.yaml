{{- if contains "externalSecret" .Values.lenses.hq.apiKey.secret.type }}
apiVersion: "external-secrets.io/v1beta1"
kind: ExternalSecret
metadata:
  name: {{ include "apiKeySecretName" . }}
spec:
  dataFrom:
    - extract:
        key: {{ .Values.lenses.hq.apiKey.secret.externalSecret.key }}
  secretStoreRef:
    kind: ClusterSecretStore
    name: {{ .Values.lenses.hq.apiKey.secret.externalSecret.secretStoreRef.clusterSecretStore.name }}
  target:
    name: {{ include "apiKeySecretName" . }}
{{- end -}}

{{- if contains "externalSecret" .Values.lensesAgent.hq.agentKey.secret.type }}
apiVersion: "external-secrets.io/v1beta1"
kind: ExternalSecret
metadata:
  name: {{ include "agentKeySecretName" . }}
spec:
  dataFrom:
    - extract:
        key: {{ .Values.lensesAgent.hq.agentKey.secret.externalSecret.name }}
  secretStoreRef:
    kind: ClusterSecretStore
    name: {{ .Values.lensesAgent.hq.agentKey.secret.externalSecret.secretStoreRef.clusterSecretStore.name }}
  target:
    name: {{ include "agentKeySecretName" . }}
{{- end -}}
å
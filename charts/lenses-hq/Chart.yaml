apiVersion: v1
description: A chart for Lenses HQ deployment which provides a unified, streamlined view of the entire event infrastructure—whether on-premises or in the cloud—through a single, comprehensive interface.
icon: https://www.lenses.io/images/logos/icon_ellipse2red.png
name: lenses-hq
version: 6.2.0
appVersion: 6.2.0
annotations:
  # Image tag used by the optional MCP sidecar (lensesio/mcp). Pinned here so
  # the MCP image cadence is decoupled from the chart/appVersion. Users can
  # override via mcp.image.tag in values.yaml.
  mcpAppVersion: "6.2.0"

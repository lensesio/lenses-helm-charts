suite: test default values in configmap
templates:
  - configmap.yaml
tests:
  - it: should work
    asserts:
      - isKind:
          of: ConfigMap
      - isEmpty:
          path: data."lenses\.append\.conf"

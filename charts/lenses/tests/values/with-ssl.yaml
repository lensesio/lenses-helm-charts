lenses:
  kafka:
    ssl:
      enabled: true
      trustStoreFileData: |-
        foo
      keyStoreFileData: |-
        bar
      trustStorePassword: fooPass
      keyStorePassword: barPass
      keyPassword: foobarPass
